{
  "name": "slack-src",
  "version": "1.0.0",
  "main": "slack.js",
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-ssm": "3.735.0",
    "@aws-sdk/client-sts": "3.734.0"
  },
  "scripts": {
    "build": "mkdir -p ../dist && zip -r ../dist/slack.zip ."
  }
}
