{
  "name": "slack-src",
  "version": "1.0.0",
  "main": "slack.js",
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-ssm": "3.645.0",
    "@aws-sdk/client-sts": "3.658.1"
  },
  "scripts": {
    "build": "mkdir -p ../dist && zip -r ../dist/slack.zip ."
  }
}
