{
  "name": "di-authentication-smoke-tests",
  "version": "1.0.0",
  "main": "src/canary.js",
  "repository": "git@github.com:alphagov/di-authentication-smoke-tests.git",
  "license": "MIT",
  "engines": {
    "node": "22.22.x",
    "npm": ">=10.0.0",
    "yarn": "Please use npm"
  },
  "scripts": {
    "clean": "rm -rf dist && rm -rf node_modules  && rm -rf release-artefacts",
    "build": "mkdir -p dist/nodejs/node_modules && cp -rf src/* dist/nodejs/node_modules/ && cp -rf node_modules/* dist/nodejs/node_modules/ && cd dist && zip -r synthetics.zip nodejs",
    "build:synthetics": "npm ci --ignore-scripts --omit=dev && npm run build",
    "build:alerts": "cd alerts && npm ci --ignore-scripts --omit=dev && npm run build",
    "build:heartbeat": "cd heartbeat && npm ci --ignore-scripts --omit=dev && npm run build",
    "build:slack": "cd slack && npm ci --ignore-scripts --omit=dev && npm run build",
    "build:all": "npm run build:synthetics && npm run build:alerts && npm run build:heartbeat && npm run build:slack",
    "pretty": "prettier --write \"**/*.{js,json}\"",
    "check-pretty": "prettier --check \"**/*.{js,json}\"",
    "lint": "eslint .",
    "test": "mocha 'test/**/*.test.js'",
    "test:coverage": "npx c8 --reporter=lcov --report-dir=coverage mocha 'test/**/*.test.js'"
  },
  "prettier": {
    "trailingComma": "es5",
    "tabWidth": 2,
    "semi": true
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1027.0",
    "@aws-sdk/client-ssm": "^3.1027.0",
    "express": "^5.1.0",
    "express-openid-connect": "^2.18.1"
  },
  "overrides": {
    "minimatch@<3.1.3": ">=3.1.3",
    "fast-xml-parser": ">=5.5.9",
    "qs": ">=6.15.2"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "c8": "^10.1.3",
    "chai": "^4.5.0",
    "eslint": "^9.39.2",
    "eslint-plugin-n": "^17.23.2",
    "globals": "^17.6.0",
    "mocha": "^11.7.5",
    "prettier": "^3.8.3",
    "sinon": "^19.0.2",
    "sinon-chai": "^3.7.0"
  }
}
