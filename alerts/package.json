{
  "name": "alerts-src",
  "version": "1.0.0",
  "main": "alerts.js",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.6.8"
  },
  "devDependencies": {
    "aws-sdk": "^2.1099.0"
  },
  "scripts": {
    "build": "mkdir -p ../dist && zip -r ../dist/alerts.zip ."
  }
}
