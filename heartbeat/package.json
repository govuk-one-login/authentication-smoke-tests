{
  "name": "heartbeat-src",
  "version": "1.0.0",
  "main": "heartbeat.js",
  "license": "MIT",
  "engines": {
    "node": "22.22.x",
    "npm": ">=10.0.0",
    "yarn": "Please use npm"
  },
  "dependencies": {
    "cronitor": "^2.6.1"
  },
  "overrides": {
    "js-yaml": "4.3.0",
    "axios": ">=1.18.0",
    "serialize-javascript": ">=7.0.5",
    "brace-expansion": ">=5.0.9"
  },
  "scripts": {
    "build": "mkdir -p ../dist && zip -r ../dist/heartbeat.zip .",
    "test": "mocha",
    "test:coverage": "npx c8 --reporter=lcov --report-dir=coverage mocha"
  },
  "devDependencies": {
    "c8": "^12.0.0",
    "chai": "^6.2.2",
    "mocha": "^11.8.0",
    "sinon": "^22.1.0",
    "sinon-chai": "^4.0.1"
  }
}
